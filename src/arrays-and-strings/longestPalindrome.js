import { assert } from "console";

/**
 * @param {string} s
 * @return {string}
 */
 var longestPalindrome = function(s) {
     return undefined;  
};

assert(longestPalindrome("test"), "this was falsy");